# Server
PORT=5000

# Database
MONGO_URI=mongodb+srv://abishekrana657_db_user:iCtmQSdE53oMNudB@cluster0.ht75yk1.mongodb.net/abhistream?retryWrites=true&w=majority
# Auth
JWT_SECRET=abhi1234

# Metadata / search / genres / info — public Vercel mirror works fine for these.
MIRURO_API_URL=https://mirurotvapi.vercel.app/api

# Streaming — REQUIRED in production for playable video.
# The public Vercel mirror's /episodes and /watch endpoints are protected by
# Cloudflare and return 403 from datacenter IPs (Render, Vercel, Railway, ...).
# Self-host the included Miruro-API on a residential/VPS IP and point this at it.
# Example: STREAM_API_URL=http://203.0.113.10:8000
# Example: STREAM_API_URL=https://miruro.your-domain.com
# Leave blank ONLY in local dev if you run `uvicorn api:app` from ./Miruro-API.
STREAM_API_URL=https://miruroapi-uli1.onrender.com

# Frontend
CLIENT_URL=https://your-frontend.onrender.com
